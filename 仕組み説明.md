# Discordログ要約Bot 仕組み説明（人に説明する用）

## 概要

Discordの「昨日のまとめ」を毎朝自動で投稿するBot。指定チャンネルの前日1日分の投稿をAIが読んで、「主要トピックス」「決定事項・アクション」「共有・お知らせ」「その他の動き」の4カテゴリに整理した日報を、毎朝まとめチャンネルへ投稿する。タイムラインを追いきれないメンバーでも、これだけ読めば昨日の動きが分かる。縁紡・明鏡・AIらぼ・クレアカ・TOLPassの5サーバーで動いている。

## 使ってるツール

| ツール | 役割 |
|---|---|
| Discord | ログの取得元＆日報の投稿先 |
| Mac の crontab | 毎朝6:00に起動する時計係（2026-08-28にGitHub Actionsから移設） |
| GitHub Actions | 手動実行（workflow_dispatch）専用の予備ルート。定時起動は停止済み |
| Claude API | ログを要約するAIの頭脳（最安クラスのHaikuモデルを使用） |
| Python | 処理を書いているプログラム言語 |

秘書のりすけと違い、GASやCloudflareは使っていない。「時計→読む→要約→投稿」の一直線構成。

## 有料 or 無料

ほぼ無料。

- Discord・GitHub Actions → 無料
- Claude API のみ有料。ただし最安クラスのモデルを1日1回使うだけなので、月数十円レベル

## 注意点

- 2026-08-28、GitHub Actionsの起動遅延が悪化した（+8時間の日もあり）ため、定時実行をMacのcrontab（毎朝6:00・`scripts/daily_summary_local.sh`）へ移した。Actions側のscheduleはコメントアウト済みで、復活させるとMac側と二重投稿になる
- Botトークン・APIキーは、Mac実行ではこのフォルダの `.env`（git管理外）、手動のActions実行ではGitHubのSecretsで管理し、コードに直接書かない
- 手で動作確認するときは `DRY_RUN=1` を付ける（取得・要約まで走らせて、Discordへの投稿だけスキップする）
- Botの権限は「チャンネル閲覧・履歴読み取り・メッセージ送信」の最小限だけ与えている
- 投稿が多すぎる日は、AIに一度に渡せる量の上限に対応するため分割要約→統合する仕組みが入っている
- README・requirements.mdには旧構成（Render.com常時稼働・朝8時投稿）の記述が残っており、現行実装（Mac crontab・朝6時）と食い違っている。説明時はこのファイルと現行コードを正とする

---

## 作業フロー（全体像）

1. まとめ役（Macのcrontab）が毎朝6:00に起動する
2. 対象5サーバーの指定チャンネルから、前日1日分のログを取得する
3. 前日に投稿がなければ「投稿はありませんでした」と通知して終了する
4. 投稿があれば、AIの頭脳（Claude・最安モデル）が4カテゴリに要約する
5. 文章が長すぎる場合は段落ごとに分割する
6. まとめチャンネルに投稿する

## 補足：メッセージが多い日の工夫

1. AIが一度に読める文章量には上限がある
2. 上限を超えそうなら、ログをいくつかの塊に分ける
3. 塊ごとに部分要約を作る
4. 部分要約を最後にまとめて、1つの要約に仕上げる
